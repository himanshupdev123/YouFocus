# YouTube Data API v3 Key
# 
# This application requires a YouTube Data API v3 key to fetch channel and video information.
# Follow these steps to obtain your API key:
#
# 1. Go to the Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the YouTube Data API v3:
#    - Navigate to "APIs & Services" > "Library"
#    - Search for "YouTube Data API v3"
#    - Click "Enable"
# 4. Create credentials (API key):
#    - Navigate to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "API Key"
#    - Copy the generated API key
# 5. (Optional but recommended) Restrict your API key:
#    - Click on the API key you just created
#    - Under "Application restrictions", select "HTTP referrers"
#    - Add your domain (e.g., localhost:5173 for development)
#    - Under "API restrictions", select "Restrict key"
#    - Choose "YouTube Data API v3"
#    - Click "Save"
#
# 6. Copy this file to .env and replace the value below with your actual API key
#
# Note: The daily quota limit is 10,000 units. This application is optimized to stay
# well within this limit through caching and efficient API usage.
#
# For more information, visit: https://developers.google.com/youtube/v3/getting-started

VITE_YOUTUBE_API_KEY=your_api_key_here
