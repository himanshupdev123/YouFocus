# YouTube Data API v3 Keys (Multiple keys for increased quota)
# 
# This application supports multiple YouTube API keys for increased quota limits.
# Each key provides 10,000 units per day, so 5 keys = 50,000 units per day.
#
# Setup Instructions:
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the YouTube Data API v3:
#    - Navigate to "APIs & Services" > "Library"
#    - Search for "YouTube Data API v3" and click "Enable"
# 4. Create multiple API keys:
#    - Navigate to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "API Key" (repeat 5 times)
# 5. Restrict each API key (recommended):
#    - Click on each API key
#    - Under "API restrictions", select "Restrict key"
#    - Choose "YouTube Data API v3" and click "Save"
#
# 6. Copy this file to .env and add your API keys below
#
# Primary API key (required)
VITE_YOUTUBE_API_KEY=your_primary_api_key_here

# Additional API keys for quota rotation (optional but recommended)
VITE_YOUTUBE_API_KEY_2=your_second_api_key_here
VITE_YOUTUBE_API_KEY_3=your_third_api_key_here
VITE_YOUTUBE_API_KEY_4=your_fourth_api_key_here
VITE_YOUTUBE_API_KEY_5=your_fifth_api_key_here

# Note: The app will automatically use all available keys and rotate between them.
# With 5 keys, you get 50,000 API units per day (500 channel searches or 50,000 other operations).
